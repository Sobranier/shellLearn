#!/bin/bash
PATH=/Users/sobranie/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:/usr/local/git/bin
export PATH

echo "Input 2 numbers,\n"
read -p "first number:" firstnum
read -p "second number:" secnum
#total=$(($firstnum*$secnum))
#echo "The result of $firstnum X $secnum is ==> $total"
